# config/packages/monolog.yaml
monolog:
    channels: ['mcp', 'app']
    handlers:
        mcp:
            type: rotating_file
            path: '%kernel.logs_dir%/mcp.log'
            level: debug
            channels: ['mcp']
            max_files: 30
