{
  "operation": "core/update",
  "class": "UserRequest",
  "key": {{id}},
  "fields": {
{% if title !== '' %}    "title":"{{title}}",{% endif %}
{% if description !== '' %}    "description":"{{description}}",{% endif %}
    "{{ log_field }}":"{{ log }}"
  },
  "output_fields": "{{output_fields}}",
  "comment": "Updated via the MCP server"
}