{
  "operation":"core/apply_stimulus",
  "class":"{{ class }}",
  "key":{{id}},
  "stimulus": "{{stimulus}}",
  "output_fields":"{{output_fields}}",
  "fields": {
{% for code, value in fields %}
    "{{ code }}":"{{ value }}"{% if not loop.last %},{% endif %}
{% endfor %}
  },
  "comment": "Created via the MCP server"
}